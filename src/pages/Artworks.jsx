import React, { useEffect, useState } from 'react';
import { base44 } from '@/api/base44Client'; import { Image } from '@/components/ui/image';
import PageHeader from '@/components/editorial/PageHeader';
const categories=['Todas','Brasil','Política','Economia','Justiça','Segurança','Mundo','Tecnologia','Sociedade','Viral'];
export default function Artworks(){const [items,setItems]=useState([]),[category,setCategory]=useState('Todas'),[date,setDate]=useState('');useEffect(()=>{base44.entities.Artwork.list('-created_date',50).then(setItems)},[]);const visible=items.filter(x=>(category==='Todas'||x.category===category)&&(!date||x.created_date?.slice(0,10)===date));return <><PageHeader title="Artes" description="Biblioteca visual organizada por pauta, data e editoria."/><div className="mb-5 flex flex-wrap gap-2"><input type="date" value={date} onChange={e=>setDate(e.target.value)} className="rounded-xl border border-white/10 bg-[#0b1424] px-3 py-2 text-sm"/><select value={category} onChange={e=>setCategory(e.target.value)} className="rounded-xl border border-white/10 bg-[#0b1424] px-3 py-2 text-sm">{categories.map(c=><option key={c}>{c}</option>)}</select></div><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-3">{visible.map(x=><article key={x.id} className="overflow-hidden rounded-2xl border border-white/7 bg-[#0b1424]"><Image src={x.image_url} alt={x.title} className="aspect-video w-full"/><div className="p-4"><span className="text-[10px] font-bold uppercase tracking-wider text-blue-400">{x.category}</span><h3 className="mt-2 font-semibold text-white">{x.title}</h3><p className="mt-2 text-xs text-slate-500">{x.status} · {new Date(x.created_date).toLocaleDateString('pt-BR')}</p></div></article>)}</div></> }